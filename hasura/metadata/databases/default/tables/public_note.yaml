table:
  name: note
  schema: public
object_relationships:
- name: user
  using:
    foreign_key_constraint_on: user_id
select_permissions:
- permission:
    columns:
    - content
    - created_at
    - id
    - location
    - user_id
    filter: {}
  role: user
