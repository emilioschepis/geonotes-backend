table:
  name: user
  schema: public
select_permissions:
- permission:
    columns:
    - id
    - created_at
    - username
    - email
    filter:
      id:
        _eq: X-Hasura-User-Id
  role: user
