type Mutation {
  create_note (
    content: String!
    latitude: Float!
    longitude: Float!
  ): CreateNoteOutput!
}

type Query {
  get_note (
    note_id: uuid!
  ): GetNoteOutput!
}

type CreateNoteOutput {
  note_id : uuid!
}

type GetNoteOutput {
  note_id : uuid!
}

